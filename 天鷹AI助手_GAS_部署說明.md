# 天鷹 AI 小助手（小天鷹）部署說明

> 目標：讓手機 APP 裡的「天鷹 AI 小助手」能跟員工對話。
> 全程 **0 成本**（Gemini 免費額度 + GAS 免費 + GitHub Pages 免費）。

---

## 一、流程長怎樣（先看懂）

```
手機 APP（tool_ai_chat.html）
  → 小天鷹 GAS（天鷹AI助手_GAS.gs）← API Key 藏在這
    → Google Gemini AI（免費）
      → 答案傳回手機，文字彈出 + 可語音朗讀
```

API Key 藏在 GAS 後端，**不會出現在網頁原始碼**，員工看不到，安全。

---

## 二、四個步驟

### 步驟 1：拿 Gemini API Key（免費，不用信用卡）

1. 打開 👉 **https://aistudio.google.com**（用你的 Gmail 登入）
2. 左邊找 **「Get API key」**
3. 按 **「Create API key」** → 選一個專案（或建新的）
4. 複製那串 key（長得像 `AIzaSy...`）先貼到記事本

> 免費額度：每分鐘 15 次、每天 1500 次，天鷹 APP 完全夠用。

---

### 步驟 2：建立小天鷹 GAS

1. 打開 👉 **https://script.google.com** → 「新增專案」
2. 把左邊預設的 `Code.gs` 內容**全刪掉**
3. 把 `天鷹AI助手_GAS.gs` 這個檔的內容**整段貼進去**
4. 專案改名叫「天鷹AI小助手」→ 存檔（Ctrl+S）

---

### 步驟 3：把 API Key 藏進 GAS

1. GAS 左邊齒輪 **「專案設定」**
2. 往下找 **「指令碼屬性」** → 「新增指令碼屬性」
3. 填：
   - 屬性（名稱）：`GEMINI_API_KEY`
   - 值：貼上步驟 1 複製的 key
4. 「儲存指令碼屬性」

---

### 步驟 4：部署成網頁

1. 右上角 **「部署」→「新增部署」**
2. 類型選 **「網頁應用程式」**
3. 設定：
   - 執行身分：**我（你的帳號）**
   - 誰可以存取：**任何人**
4. 按「部署」→ 第一次會跳授權，照著允許（選你的帳號 → 進階 → 前往 → 允許）
5. 複製最後給你的 **網頁應用程式網址**（結尾是 `/exec`）

---

## 三、把網址填進工具

打開 `tool_ai_chat.html`，找到最上面這一行：

```javascript
var GAS_URL = 'YOUR_GAS_URL_HERE';
```

把 `YOUR_GAS_URL_HERE` 換成你步驟 4 複製的 `/exec` 網址，存檔。

> 跟我（Claude Code）說「GAS 網址是 xxx」，我直接幫你填好也行。

---

## 四、測試

1. 推上 GitHub → 等 GitHub Pages 更新（約 1 分鐘）
2. 手機開 APP → 首頁點「🦅 天鷹 AI 小助手」
3. 打字問「事故報告怎麼填？」→ 小天鷹會從底部彈出答案
4. 按 🎤 → 允許麥克風 → 用講的問 → 它會唸答案給你聽

---

## 五、管理員專屬功能

當你（管理員）發現小天鷹講了不該講的話：

1. 工具右上角會有 **⚙️ 齒輪**（只有管理員看得到）
2. 點進去可以：
   - **禁止話題**：輸入「不可以講的事」（例：不可以透露員工薪水），一行一條
   - **強制表達層級**：要它一律講最簡單、還是標準口語
3. 儲存後，所有員工下次問小天鷹就會自動套用

---

## 六、改了 GAS 之後（重要）

以後若修改 `天鷹AI助手_GAS.gs`：
- 一定要 **「部署 → 管理部署 → 編輯（鉛筆）→ 版本選「新版本」→ 部署」**
- **不要**「新增部署」（會產生新網址，工具就連不到了）

---

## 七、小天鷹的個性設定（已內建）

- **名字**：小天鷹（對員工自我介紹用）
- **講話**：口語化、說人話，年紀大或學歷不高的同事也聽得懂
- **誠實**：查不到就老實說「不清楚，建議問主管」，絕不亂掰
- **會反問**：問題不清楚時主動問你是指哪個
- **記習慣**：每個工號用越多次，越懂得用適合他的方式回答
- **分權限**：一般員工 / 幹部 / 主管，能問的範圍不同，主管全開但仍守保密原則
