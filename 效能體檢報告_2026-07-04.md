# 天鷹保全 APP 效能體檢報告（2026-07-04）

> 檢查範圍：index.html 主程式 + 13 支工具頁。方法：程式碼靜態掃描 + 沙盒 Chromium 實機載入測試（模擬 4 倍降速中階手機、iPhone 尺寸 390x844）。

---

## 一、實測數據

| 項目 | 數值 | 評價 |
|------|------|------|
| index.html 檔案大小 | **3.3MB**（gzip 後 1.8MB） | 🔴 過大 |
| DOM 下載＋解析 | 1.9 秒（本機零網路延遲） | 🔴 |
| Splash 消失／畫面可互動 | 2.9 秒（本機） | 🟡 |
| 真實手機 4G 首次開啟估算 | **4~7 秒** | 🔴 |
| 分頁切換（首頁→工具） | 168ms | 🟢 順 |
| 開 base64 內嵌工具（打烊） | 約 1 秒 | 🟡 |
| JS 記憶體用量 | 14MB | 🟢 健康 |
| 登入時 GAS 呼叫數（主管帳號） | 7~8 個（每個冷啟 1~3 秒） | 🟡 平行發射還行，可合併 |

補充：GitHub Pages 的 HTML 快取只有 10 分鐘，代表同事**每隔一陣子就要重新下載 1.8MB**，檔案肥大的傷害會一直重複發生。

---

## 二、肥大根因：LOGO 圖片就吃掉 2MB（最大炸彈 💣）

| 位置 | 內容 | 浪費 |
|------|------|------|
| index.html | 老鷹 LOGO **原圖 1120x980px**（371KB）以 base64 內嵌，且 `EAGLE_SRC` 定義了**兩次** | 991KB |
| tpl-upload / tpl-signin / tpl-car（index 內嵌模板） | 各藏一份 617x554px LOGO（270KB），base64 再包 base64 膨脹成約 360KB | 約 1MB |
| tool_work / post / tool_report / tool_feedback / liff_leave | 每支檔案內嵌同一張 617x554 LOGO **兩次**（各 540KB） | 每檔 540KB |

事實對照：repo 裡的 `icon.png` 才 **21KB**，畫面上 LOGO 實際顯示大概 64~120px。

**修法**：LOGO 壓成一張約 200x200、15~20KB 的圖，全部改引用同一個檔（`./icon.png` 路徑或單一份小 base64）。
**預期效果**：
- index.html：3.3MB → 約 **1.3MB**（gzip 後約 0.4MB）
- 每支工具檔：約 590KB → 約 **50KB**
- 手機首次開啟：4~7 秒 → 約 **1.5~2.5 秒**

---

## 三、按鍵與流程檢查發現的 Bug

### 🐛 Bug 1：啟動時會跳「請假清單連線失敗」錯誤 Toast（實測直接看到）
- `loadLeaveFromCloud(true)` 用靜默模式在背景載入，但**失敗分支沒判斷 silent**（index.html 約 19358、19361 行），網路稍慢或斷線就對所有人跳紅字錯誤，體感很差。
- 修法：兩個錯誤分支加上 `if (!silent)`。

### 🐛 Bug 2：錯誤 Toast 從來沒有紅色樣式
- 很多地方呼叫 `showToast(msg, 'err')`，但 `showToast` 的定義只收一個參數（約 19284 行），第二個 `'err'` 直接被丟掉 → 所有錯誤提示都長得跟成功一樣。
- 這違反自家規範「失敗用紅 #F87171」。修法：showToast 加 type 參數並套紅色樣式。

### 🟡 觀察 3：5 個 base64 工具模板在啟動時全部同步解碼
- `TOOL_UPLOAD_HTML` 等 5 個常數在頁面載入當下就把約 1.5MB 的 base64 全部解碼（約 17253~17263 行），即使這次根本不會開那些工具。
- 修法：改成第一次點開該工具才解碼（lazy decode），啟動再快一截。

### 🟢 按鍵反應本身沒問題
- 分頁切換 168ms、返回鈕 106ms，React 渲染順暢；記憶體健康無洩漏跡象。按鈕點擊卡頓的體感主要來自「開工具要等 iframe 載入」與「等 GAS 回應」，不是前端渲染慢。

---

## 四、資料讀取速度分析

1. **登入時 ACCOUNT_GAS 被打 5 次**（getApplications / getLeaveRequests / getSettings / getAnnouncements / getApprovedUsers）＋ REPORT_GAS 2 次（getReports / getFeedback）。好消息是平行發射；但同一支 GAS 的 5 個 action 可以在後端合併成一個 `action=bootstrap` 一次回全部 → 主管登入的資料等待時間從「最慢那一個 × 排隊」變成一個往返，體感快 2~4 秒。
2. **公告每 2 分鐘輪詢**，APP 掛在背景也照打。加一行 `if (document.visibilityState !== 'visible') return;` 可以省掉大量無效 GAS 呼叫（GAS 免費額度是共用的，人多的時候會互相拖慢）。
3. **施工單（tool_work）每次開啟都重抓 gviz、沒有快取**。報到狀態要即時所以不能只用快取，但可以改成「先顯示上次的快取資料、背景抓新資料回來再更新」（stale-while-revalidate），畫面秒開、資料照樣新。
4. **SheetJS（約 900KB）在 index.html 的 `<head>` 同步載入**，會阻塞第一次畫面渲染，但它只有「班表 Excel 匯入」這個使用者手動觸發的功能會用到。`<script src>` 加上 `defer` 一個字就解決。

---

## 五、優先級優化清單

| 優先 | 項目 | 效益 | 工時估 | 風險 |
|------|------|------|--------|------|
| **P0** | LOGO 圖片瘦身統一（index 兩份大圖 + 各工具兩份 + 3 個內嵌模板） | 全站省約 **2MB**，首載快 3~5 秒 | 2~3 小時 | 低（純換圖） |
| **P0** | SheetJS 加 `defer` | 首次渲染不被 900KB 外部 JS 卡住 | 5 分鐘 | 低 |
| **P1** | 修 Bug 1（silent 錯誤 Toast）＋ Bug 2（err 紅色樣式） | 啟動不再莫名跳錯誤；錯誤提示合規變紅 | 30 分鐘 | 低 |
| **P2** | ACCOUNT_GAS 加 `action=bootstrap` 合併登入 5 個呼叫 | 主管登入資料到位快 2~4 秒 | 2~3 小時（前後端） | 中（GAS 要重新部署） |
| **P2** | 公告輪詢加 visibility 判斷 | 省 GAS 額度、省手機電量 | 15 分鐘 | 低 |
| **P2** | base64 模板 lazy 解碼 | 啟動 JS 執行再省一截 | 1 小時 | 低 |
| **P2** | 施工單 gviz 加 stale-while-revalidate 快取 | 施工單畫面秒開 | 1~2 小時 | 低 |
| **P3** | base64 內嵌工具逐步改獨立檔（CLAUDE.md 已列的維護痛點） | git diff 可用、index 再瘦 1.5MB | 分批進行 | 中 |
| **P3** | 主程式 JS minify（目前含註解未壓縮，約 1.8MB） | 再省 50~60% JS 體積 | 需引入建置流程 | 中 |

**建議順序**：P0 兩項先做（一個下午，全站體感直接升級）→ P1 修 bug → P2 挑「公告 visibility」和「bootstrap 合併」。P3 不急，配合日後改版順手做。

---

*測試工具：Playwright + 沙盒 Chromium（4x CPU throttle）；GAS 端點在沙盒被代理阻擋，GAS 延遲為業界典型值估算，實際數字建議部署後用手機開 F12 遠端偵錯確認。*
