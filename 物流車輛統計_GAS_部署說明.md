# 物流車輛統計工具 — GAS 部署說明

**工具**：物流車輛統計（`tool_logistics.html`）
**目標試算表**：新建一份（建議命名「物流車輛統計」）
**分頁名稱**：`物流車輛紀錄`（主資料）、`快捷設定`（管理員快捷組合）——皆由 GAS 第一次使用時自動建立，含表頭

---

## 架構：單一獨立 GAS

物流車輛統計是獨立系統，不共用其他工具的 GAS 或試算表：

| 連線 | 變數 | 指向 | 負責 |
|------|------|------|------|
| 唯一連線 | `BUILT_IN_GAS_URL` | **物流 GAS URL**（本說明要新部署） | 登記寫入、單日查詢、整月彙總、修改/刪除、產生月統計分頁 |

**資料流**：登記（分類＋數量）→ GAS 蓋伺服器時間戳寫入「物流車輛紀錄」→ 查詢/月統計從同分頁彙總 → `exportMonth` 在同一份試算表產生「YYYY-MM 月統計」分頁（每天 × 三分類 ＋ 合計，可直接列印交報表）。

---

## 部署步驟（約 3 分鐘）

### 1. 新建試算表
Google 雲端硬碟 → 新增 → Google 試算表 → 命名「物流車輛統計」。
分頁不用手動建，GAS 會自動建立 `物流車輛紀錄` 並填好表頭。

### 2. 開啟 Apps Script
試算表 → 上方選單 **擴充功能 → Apps Script**

### 3. 貼上程式碼
刪掉預設的 `function myFunction(){}`，貼上 `物流車輛統計_GAS.gs` 的完整程式碼（也可從工具內 **設定 → GAS 程式碼 → 一鍵複製**）。

### 4. 部署為網頁應用程式
- 右上 **部署 → 新增部署**
- 類型選 **網頁應用程式（Web app）**
- 「執行身分」：**我（你的帳號）**
- 「誰可以存取」：**任何人（Anyone）**
- 按 **部署**，第一次會要求授權 → 允許

### 5. 複製網址貼回工具
- 複製產生的 **`/exec` 結尾網址**
- 開啟物流車輛統計工具 → **設定** 頁 → 貼到「GAS 連線網址」→ 按儲存測試
- （或把網址給我，我直接寫進 `tool_logistics.html` 的 `BUILT_IN_GAS_URL`，所有員工開啟即自動連線）

### 6. 之後改 GAS 的鐵則
改任何程式碼後：**部署 → 管理部署 → 編輯（鉛筆）→ 版本：新版本 → 部署**。
**不可**「新增部署」——網址會變，前端全斷。

---

## 資料表結構

**`物流車輛紀錄`**（主記錄，A~H 欄，自動建立）

| A 紀錄ID | B 日期 | C 時間 | D 分類 | E 數量 | F 登記人工號 | G 登記人姓名 | H 建立時間 |
|---|---|---|---|---|---|---|---|
| 純數字流水號 | yyyy/M/d | HH:mm:ss | 1.9噸 / 3.5噸 / 8噸以上 | 整數 | 純文字（保留開頭 0） | 文字 | yyyy/M/d HH:mm:ss |

> 紀錄ID 為**純數字流水號**，由 GAS 取「既有最大 ID + 1」產生（因支援刪除列，不可用列號，否則會重號），符合天鷹保全主鍵規範。
> 登記時間一律由 GAS 伺服器端蓋「送出當下」的時間戳，前端不傳時間。

**`快捷設定`**（管理員在工具「設定」頁維護，與主資料分開，自動建立）

| A 快捷ID | B 分類 | C 數量 |
|---|---|---|
| 純數字流水號 | 1.9噸 / 3.5噸 / 8噸以上 | 整數 |

> 管理員在設定頁新增／編輯／刪除快捷組合（如 1.9噸 × 2），全員的登記頁會出現對應快捷按鈕，點一下直接送出登記，雲端即時同步。

**`YYYY-MM 月統計`**（按鈕產生，可重複產生覆蓋更新）

| 日期 | 1.9噸 | 3.5噸 | 8噸以上 | 合計 |
|---|---|---|---|---|
| 7/1 … 7/31（含沒資料的日子補 0） | | | | |
| **合計** | | | | |

---

## API 一覽

| 方法 | action | 參數 | 用途 |
|------|--------|------|------|
| GET | （無） | — | 連線測試，回「天鷹保全 物流車輛統計 API 正常 ✓」 |
| POST | `add` | category, count, empId, name | 新增登記（伺服器蓋時間戳） |
| POST | `update` | id, count | 修改數量 |
| POST | `delete` | id | 刪除紀錄 |
| GET | `getDay` | date=YYYY-MM-DD | 單日逐筆 ＋ 三分類合計 |
| GET | `getMonth` | month=YYYY-MM | 整月每日 × 三分類彙總 |
| POST | `exportMonth` | month=YYYY-MM | 產生/更新「YYYY-MM 月統計」分頁 |
| GET | `getShortcuts` | — | 快捷組合清單（全員登記頁顯示用） |
| POST | `addShortcut` | category, count | 新增快捷組合 |
| POST | `updateShortcut` | id, category, count | 修改快捷組合 |
| POST | `deleteShortcut` | id | 刪除快捷組合 |

前後端通訊走 URLSearchParams（簡單請求，無 CORS 預檢），回應統一 `{status:'ok'|'error', msg, ...}`。

---

## 驗證清單

- [ ] GAS 部署成功，`/exec` 網址可開啟並回 `天鷹保全 物流車輛統計 API 正常 ✓`
- [ ] 工具設定頁貼上網址後顯示「✓ 連線成功」
- [ ] 登記一筆 → 「物流車輛紀錄」新增一列，A 欄為純數字（1、2、3…）、B/C 欄為送出當下日期時間
- [ ] 工具「登記」頁今日累計立即更新
- [ ] 「查紀錄」選今天 → 看得到剛登記那筆，✏️ 改數量、🗑 刪除都生效
- [ ] 「查看整月」→ 每日 × 三分類表格正確
- [ ] 「產生試算表月統計分頁」→ 試算表出現「YYYY-MM 月統計」分頁，數字與工具內一致
- [ ] 工號開頭 0 沒被吃掉（F 欄為純文字格式）
- [ ] 設定頁新增快捷（如 1.9噸 × 2）→ 「快捷設定」分頁新增一列、登記頁出現快捷按鈕
- [ ] 點快捷按鈕 → 直接登記成功、今日累計更新
- [ ] 編輯／刪除快捷 → 分頁與登記頁按鈕同步更新
