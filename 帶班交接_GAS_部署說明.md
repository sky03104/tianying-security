# 帶班交接事項工具 — GAS 部署說明

**工具**：帶班交接事項（`tool_handover.html`）
**目標試算表**：新建一份（建議命名「帶班交接事項」）
**分頁名稱**：`交接事項`（主資料）——由 GAS 第一次使用時自動建立，含表頭

---

## 架構：單一獨立 GAS

帶班交接事項是獨立系統，不共用其他工具的 GAS 或試算表：

| 連線 | 變數 | 指向 | 負責 |
|------|------|------|------|
| 唯一連線 | `BUILT_IN_GAS_URL` | **交接事項 GAS URL**（本說明要新部署） | 新增/編輯/刪除事項、狀態切換、讀清單 |

**資料流**：新增事項（內容）→ GAS 蓋伺服器時間戳寫入「交接事項」（狀態預設「未完成」）→ 編輯內容或切換狀態（未完成/進行中/已完成）皆更新「最後修改人/時間」→ 全員（組長以上）共用同一份清單即時同步。

---

## 部署步驟（約 3 分鐘）

### 1. 新建試算表
Google 雲端硬碟 → 新增 → Google 試算表 → 命名「帶班交接事項」。
分頁不用手動建，GAS 會自動建立 `交接事項` 並填好表頭。

### 2. 開啟 Apps Script
試算表 → 上方選單 **擴充功能 → Apps Script**

### 3. 貼上程式碼
刪掉預設的 `function myFunction(){}`，貼上 `帶班交接_GAS.gs` 的完整程式碼。

### 4. 部署為網頁應用程式
- 右上 **部署 → 新增部署**
- 類型選 **網頁應用程式（Web app）**
- 「執行身分」：**我（你的帳號）**
- 「誰可以存取」：**任何人（Anyone）**
- 按 **部署**，第一次會要求授權 → 允許

### 5. 複製網址填回工具
- 複製產生的 **`/exec` 結尾網址**
- 把網址給我（Claude Code），我直接寫進 `tool_handover.html` 的 `BUILT_IN_GAS_URL`，所有組長以上人員開啟即自動連線
- 個人測試（尚未填 `BUILT_IN_GAS_URL` 前）：瀏覽器開發者工具 Console 執行
  `localStorage.setItem('hsh_handover_gasUrl', '你的 /exec 網址')`

### 6. 之後改 GAS 的鐵則
改任何程式碼後：**部署 → 管理部署 → 編輯（鉛筆）→ 版本：新版本 → 部署**。
**不可**「新增部署」——網址會變，前端全斷。

---

## 資料表結構

**`交接事項`**（主記錄，A~I 欄，自動建立）

| A 事項ID | B 事項內容 | C 狀態 | D 建立人工號 | E 建立人姓名 | F 建立時間 | G 最後修改人工號 | H 最後修改人姓名 | I 最後修改時間 |
|---|---|---|---|---|---|---|---|---|
| 純數字流水號 | 文字（可多行） | 未完成/進行中/已完成 | 純文字（保留開頭 0） | 文字 | yyyy/M/d HH:mm:ss | 純文字 | 文字 | yyyy/M/d HH:mm:ss |

> 事項ID 為**純數字流水號**，由 GAS 取「既有最大 ID + 1」產生（因支援刪除列，不可用列號，否則會重號），符合天鷹保全主鍵規範。
> 新增時建立時間與最後修改時間相同（伺服器端「送出當下」）；之後任何內容編輯或狀態切換都會更新「最後修改人/時間」，但不會動「建立人/建立時間」。

---

## API 一覽

| 方法 | action | 參數 | 用途 |
|------|--------|------|------|
| GET | （無） | — | 連線測試，回「天鷹保全 帶班交接事項 API 正常 ✓」 |
| POST | `add` | content, empId, name | 新增事項（狀態預設「未完成」，伺服器蓋時間戳） |
| POST | `update` | id, content?, status?, empId, name | 編輯內容 and/or 切換狀態（未帶的欄位不動），寫入最後修改人/時間 |
| POST | `delete` | id | 刪除事項 |
| GET | `getAll` | — | 讀全部事項（新的在前） |

前後端通訊走 URLSearchParams（簡單請求，無 CORS 預檢），回應統一 `{status:'ok'|'error', msg, ...}`。

---

## 權限規劃

- **App 選單層級**：`index.html` `DEFAULT_PERMS` 只把工具 id 19 加進 `leader/vicecaptain/captain/executive/admin`，一般保全員（`fulltime`/`parttime`）選單看不到「帶班交接事項」卡片。
- **工具內層級**：`tool_handover.html` 的 `checkRole()` 讀 `localStorage.hsh_session_user.role`，非組長以上且非獨立開啟（`window.self===window.top`，供部署測試用）時顯示「權限不足」畫面，防止繞過 App 直接開網址存取。

---

## 驗證清單

- [ ] GAS 部署成功，`/exec` 網址可開啟並回 `天鷹保全 帶班交接事項 API 正常 ✓`
- [ ] 新增一筆事項 → 「交接事項」新增一列，A 欄為純數字（1、2、3…）、C 欄為「未完成」、F/I 欄時間相同
- [ ] 編輯事項內容 → B 欄更新，G/H/I（最後修改）更新，D/E/F（建立）不變
- [ ] 切換狀態（未完成→進行中→已完成）→ C 欄更新，G/H/I 同步更新
- [ ] 刪除事項 → 該列從試算表移除
- [ ] 狀態分頁籤（全部/未完成/進行中/已完成）篩選正確
- [ ] 工號開頭 0 沒被吃掉（D/G 欄為純文字格式）
- [ ] 組長以上帳號登入 → App 選單看得到「帶班交接事項」卡片；一般保全員帳號看不到
- [ ] 直接開 `tool_handover.html?...` 網址（模擬繞過 App）、`localStorage.hsh_session_user.role` 設為一般員工 → 顯示「權限不足」畫面
