# 無線電管理 GAS 部署說明

工具：`tool_radio.html`（前端）＋ `無線電管理_GAS.gs`（後端，獨立部署／獨立試算表）
主選單：id 24「無線電管理」，資料管理分類，**組長以上**可見。

---

## 一、建立試算表

1. Google Drive → 新增 → Google 試算表，取名「無線電管理」
2. 從網址列複製試算表 ID：
   `https://docs.google.com/spreadsheets/d/`**`這一段就是 ID`**`/edit`
3. 兩個分頁（`無線電`、`異動紀錄`）不用自己建，程式第一次執行會自動建好並套用標題列樣式。

## 二、建立 GAS 專案

1. 該試算表 → 擴充功能 → Apps Script
2. 把 `無線電管理_GAS.gs` 全部內容貼進 `程式碼.gs`（原本的 `function myFunction(){}` 刪掉）
3. 把最上面的 `var SPREADSHEET_ID = '請填入試算表ID';` 換成步驟一複製的 ID
4. 儲存（Ctrl+S）

## 三、部署

1. 右上「部署」→「新增部署作業」
2. 類型選「網路應用程式」
3. 執行身分：**我**；具有存取權的使用者：**所有人**
4. 部署 → 第一次會要求授權，選自己的 Google 帳號 →「進階」→「前往（不安全）」→ 允許
5. 複製「網路應用程式」網址（`https://script.google.com/macros/s/AKfy.../exec`）

## 四、接上前端

把上一步複製的網址填進 `tool_radio.html` 最上面：

```javascript
var RADIO_GAS_URL = '請填入無線電管理GAS部署網址';   // ← 換成剛剛複製的網址
```

改完 commit + push，GitHub Pages 會自動更新。

## 五、匯入現有的 95 台（只做一次）

1. Apps Script 編輯器上方函式下拉選單，選 **`初始匯入`**，按「執行」
2. 執行紀錄出現「已匯入 95 台無線電」就完成了
3. 這支函式有防呆：清冊裡只要已經有資料就會直接中止，不會重複匯入

匯入來源是「無線電編列登錄.xlsx」三個分頁去重合併後的現況（優先序：
無線電持有人 > 故障繳回編號 > 無線電使用狀況舊表），以機身編號為唯一鍵。

| 狀態 | 台數 | 說明 |
|---|---|---|
| 使用中 | 43 | 有登記使用者 |
| 公用機 | 10 | 不掛在特定人名下 |
| 報廢 | 14 | 原「報廢箱子」 |
| **待盤點** | **14** | ⚠️ 只出現在舊表、新表沒收錄，見下 |
| 繳回公司 | 8 | |
| 未找到 | 3 | |
| 庫存 | 2 | 未撥發 |
| 維修中 | 1 | 訊號不良的公用機 |

⚠️ **「待盤點」那 14 台要現場確認**：不確定是已換新機還是漏登記，
原本登記的使用者姓名已保留在備註欄（「原登記使用者：○○○」）。
盤點後用工具的「改狀態／撥發」更新即可。

## 六、之後要改程式

部署 →「管理部署作業」→ 鉛筆圖示 → 版本選「**新版本**」→ 部署。

⚠️ **不要按「新增部署作業」**，網址會變，前端會整個連不上。

---

## 權限說明

- 可用角色：組長／副隊長／隊長／公司主管／管理員
- 三層把關：`index.html` 的 `DEFAULT_PERMS`（決定看不看得到入口）→
  `tool_radio.html` 的 `checkPermission()`（前端擋）→ GAS 每個 action
  驗 token＋角色。**前兩層都在使用者的瀏覽器裡、改得掉，後端這層才是真的。**
- token 驗證（2026-09-25 改版，跟缺班調班紀錄同一套）：優先「本機驗證」（HMAC 簽章＋
  直接讀主 App 試算表的「帳號管理」），通過結果快取 5 分鐘；沒設定 `SESSION_SECRET`
  時才退回打主 App 的 GAS（`MAIN_APP_GAS_URL_`）確認，會慢好幾秒。
  **要走快速的本機驗證**：主 App 專案 → 專案設定 → 指令碼屬性 → 複製 `SESSION_SECRET` 的值 →
  本專案 → 專案設定 → 指令碼屬性 → 新增同名 `SESSION_SECRET` 貼上 → 編輯器選 `forceAuth` 按執行一次
  （授權讀主 App 試算表，執行紀錄會顯示「已設定：走本機驗證（快）」）→ 部署新版本。
